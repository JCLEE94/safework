apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: safework
type: kubernetes.io/dockerconfigjson
data:
  # GitHub Personal Access Token으로 생성해야 함
  # echo -n '{"auths":{"ghcr.io":{"username":"YOUR_GITHUB_USERNAME","password":"YOUR_GITHUB_TOKEN","auth":"BASE64_ENCODED_USERNAME:TOKEN"}}}' | base64 -w 0
  .dockerconfigjson: # PLACEHOLDER - GitHub Token으로 생성 필요